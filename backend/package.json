{
  "name": "backend",
  "version": "0.1.0",
  "description": "viscalaterra.cat — API backend",
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "rimraf dist && tsc",
    "start": "node dist/index.js",
    "seed": "ts-node-dev --transpile-only src/scripts/seed-geodata.ts"
  },
  "license": "AGPL-3.0-only",
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^25.9.1",
    "@types/pg": "^8.20.0",
    "rimraf": "^6.1.3",
    "ts-node-dev": "^2.0.0",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.0.0",
    "pg": "^8.21.0"
  }
}
